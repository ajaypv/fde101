---
title: RAG, end to end
description: Learn retrieval-augmented generation as an evidence pipeline, with provider-neutral Python and practical failure modes.
contentType: lesson
level: Beginner
minutes: 19
topics: [RAG, retrieval, citations, grounding]
lastVerified: 2026-08-15
sidebar:
  order: 1
sources:
  - title: Retrieval-augmented generation for knowledge-intensive NLP tasks
    url: https://arxiv.org/abs/2005.11401
    publisher: arXiv
    type: paper
  - title: Retrieval overview
    url: https://docs.langchain.com/oss/python/langchain/retrieval
    publisher: LangChain
    type: official-doc
---

import { Code } from '@astrojs/starlight/components';
import LessonMeta from '../../../components/LessonMeta.astro';
import Takeaway from '../../../components/Takeaway.astro';
import ConceptFlow from '../../../components/ConceptFlow.astro';
import ragCode from '../../../examples/rag/minimal.py?raw';

<LessonMeta level="Beginner" minutes={19} verified="15 Aug 2026" />

<Takeaway>RAG retrieves evidence at request time and gives that evidence to a model so the answer can be grounded in information outside its training.</Takeaway>

Ask: “What is our refund window?” The useful answer is in the company policy, not necessarily in the model’s training data.

RAG describes the **retrieve, then generate from evidence** pattern.[^rag-paper] It does not prescribe one retrieval technology. The evidence can come from keyword search, vector search, SQL, an API, or a combination. Embeddings and vector databases are common tools, not part of the definition.

<ConceptFlow steps={['Question', 'Retrieve', 'Assemble context', 'Generate', 'Validate and cite']} label="RAG request flow" />

## The smallest honest implementation

This example hides vendor details behind two tiny interfaces. The important parts remain visible: retrieval depth, source labels, an abstention instruction, and citation markers.

<Code code={ragCode} lang="python" title="src/examples/rag/minimal.py" />

The code does **not** prove the answer is correct. It creates a testable path from question to evidence to answer.

## Offline indexing and online answering

RAG has two different lifecycles.[^retrieval]

| Lifecycle | Typical work | Typical failure |
| --- | --- | --- |
| **Indexing** | Load, clean, split, enrich, and make documents searchable | Stale, duplicated, badly parsed, or permission-blind content |
| **Answering** | Understand the question, retrieve passages, build context, generate, cite | Wrong query, weak ranking, missing evidence, unsupported claim |

Keeping these lifecycles separate makes incidents easier to diagnose. A prompt change cannot repair a PDF that was never parsed correctly.

## Debug in evidence order

1. Was the correct source indexed and current?
2. Did the retriever return the relevant passage?
3. Was that passage preserved in the final context?
4. Did the model use it accurately?
5. Does the citation point to the passage that supports the claim?

## Common design choices

| Choice | Start simple | Add complexity when… |
| --- | --- | --- |
| Retrieval | One searchable source | Queries need filters, exact matches, semantic matches, or multiple sources |
| Ranking | The retriever's first ranking | Top results are related but not answer-bearing |
| Generation | One grounded call | The task requires tools, branching, approval, or recovery |
| Citations | Passage-level source IDs | Users need page, section, timestamp, or access-controlled links |

## Questions to ask in the field

- Who owns source freshness and deletion?
- Must retrieval enforce each user’s document permissions?
- What is a correct abstention for this workflow?
- How will support staff inspect the retrieved passages?
- Which representative questions will become the first evaluation set?

A small pipeline is an honest baseline, and it can be enough for a narrow production use case when it meets its gates. When measured failures justify more stages, continue to [production retrieval](./production-retrieval/) for query rewriting, hybrid search, reranking, permission filters, and stage-by-stage traces.

[^rag-paper]: Lewis et al., [“Retrieval-Augmented Generation for Knowledge-Intensive NLP Tasks”](https://arxiv.org/abs/2005.11401), introduced the RAG formulation using retrieved external memory.
[^retrieval]: LangChain’s [retrieval documentation](https://docs.langchain.com/oss/python/langchain/retrieval) describes indexing as a separate process from runtime retrieval and generation.
